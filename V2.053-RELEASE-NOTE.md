# Savingio Factory V2.053

## Stable single-click release

- Added one permanent launcher: `00-START-FACTORY.bat`.
- The launcher reads the current version from `VERSION.json`; the user no longer needs to find a version-specific BAT file.
- Runs warning-free regression, deprecated API scan, safe scoped Git release, push, and Cloudflare/live-site verification in one flow.
- Writes a unified result to `factory/output/start_factory_report.json`.
- Publisher ID remains locked to `pub-7605193583747751` through the existing single-source configuration.
