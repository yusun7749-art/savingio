name: Cleanup salary duplicate URLs

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/cleanup-salary-duplicate-urls.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Remove retired salary URLs from sitemap
        run: |
          python - <<'PY'
          from pathlib import Path
          path = Path('sitemap.xml')
          text = path.read_text(encoding='utf-8')
          retired = [
              'https://savingio.com/articles/salary-management-guide.html',
              'https://savingio.com/articles/salary-budget-guide.html',
          ]
          for url in retired:
              text = text.replace(f'  <url><loc>{url}</loc></url>\n', '')
          path.write_text(text, encoding='utf-8')
          PY
      - name: Commit sitemap cleanup
        run: |
          if git diff --quiet -- sitemap.xml; then
            echo 'No sitemap changes required.'
            exit 0
          fi
          git config user.name 'savingio-bot'
          git config user.email 'savingio-bot@users.noreply.github.com'
          git add sitemap.xml
          git commit -m 'Part017 remove salary duplicate URLs from sitemap'
          git push
