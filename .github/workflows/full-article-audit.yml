name: Full Article Audit

on:
  push:
    branches: [main]
    paths:
      - 'factory/full_article_audit.py'
      - 'factory/second_link_audit.py'
      - 'factory/unique_404_pattern_audit.py'
      - '.github/workflows/full-article-audit.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run full article audit
        run: python factory/full_article_audit.py
      - name: Run second-stage link audit
        run: python factory/second_link_audit.py
      - name: Run unique 404 pattern audit
        run: python factory/unique_404_pattern_audit.py
      - name: Commit audit reports
        run: |
          if [ -z "$(git status --porcelain factory/FULL_ARTICLE_AUDIT factory/SECOND_LINK_AUDIT factory/UNIQUE_404_PATTERN_AUDIT)" ]; then
            echo 'No report changes.'
            exit 0
          fi
          git config user.name 'savingio-audit-bot'
          git config user.email 'actions@users.noreply.github.com'
          git add -f factory/FULL_ARTICLE_AUDIT factory/SECOND_LINK_AUDIT factory/UNIQUE_404_PATTERN_AUDIT
          git commit -m 'Audit: refresh article, link, and 404 pattern reports [skip ci]'
          git push
