name: Cleanup VAT and water duplicate URLs

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/cleanup-vat-water-duplicate-urls.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Remove retired duplicate URLs from sitemap
        run: |
          python - <<'PY'
          from pathlib import Path
          path = Path('sitemap.xml')
          text = path.read_text(encoding='utf-8')
          retired = [
              'https://savingio.com/articles/vat-final-return-july-2026.html',
              'https://savingio.com/articles/vat-zero-sales-filing.html',
              'https://savingio.com/articles/water-bill-guide.html',
          ]
          for url in retired:
              text = text.replace(f'  <url><loc>{url}</loc></url>\n', '')
          path.write_text(text, encoding='utf-8')
          PY
      - name: Commit sitemap cleanup
        run: |
          if git diff --quiet -- sitemap.xml; then
            echo 'No sitemap changes required.'
            exit 0
          fi
          git config user.name 'savingio-bot'
          git config user.email 'savingio-bot@users.noreply.github.com'
          git add sitemap.xml
          git commit -m 'Part018 remove VAT and water duplicate URLs from sitemap'
          git push
