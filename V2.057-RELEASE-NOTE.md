# Savingio Factory V2.057

- Generated HTML is always preserved in `factory/output/drafts`.
- Content is copied to `articles` only when research, QA, and department gates pass.
- Ready content is added to `factory/output/cms_release_queue.json`.
- Blocked content receives `factory/output/content_review_required.json` instead of disappearing.
- `01-START-CONTENT-FACTORY.bat` opens the result report automatically.
