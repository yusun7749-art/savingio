(()=>{
'use strict';
const DATA_URL='/data/savingio-brain-data.json?v=20260731-directory2';
const PAGE_SIZE=12;
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function flatten(data){const out=[],seen=new Set();Object.entries(data.tree||{}).forEach(([large,middles])=>Object.entries(middles||{}).forEach(([middle,smalls])=>Object.entries(smalls||{}).forEach(([small,items])=>(items||[]).forEach(item=>{if(!item?.title||!item?.href||item.type==='hub'||seen.has(item.href))return;seen.add(item.href);out.push({title:item.title,href:item.href,category:middle,large,small,description:item.description||''});}))));return out;}
async function init(){const grid=document.getElementById('articleGrid'),count=document.getElementById('resultCount'),pager=document.querySelector('.pager');if(!grid||!count)return;let records=[];try{const res=await fetch(DATA_URL,{cache:'no-store'});if(!res.ok)throw new Error(String(res.status));records=flatten(await res.json());}catch(e){count.textContent='정보를 불러오지 못했습니다.';grid.innerHTML='<p>잠시 후 다시 시도해 주세요.</p>';return;}let page=1;function render(){const pages=Math.max(1,Math.ceil(records.length/PAGE_SIZE));page=Math.min(page,pages);const visible=records.slice((page-1)*PAGE_SIZE,page*PAGE_SIZE);grid.innerHTML=visible.map(r=>`<a class="article-card" href="${esc(r.href)}"><span class="card-category">${esc(r.category||r.large||'생활정보')}</span><h2>${esc(r.title)}</h2><p>${esc(r.description||'관련 내용을 확인합니다.')}</p><b>읽기 →</b></a>`).join('')||'<p>표시할 생활정보가 없습니다.</p>';count.textContent=`전체 생활정보 ${records.length}개`;if(pager)pager.innerHTML=pages>1?Array.from({length:pages},(_,i)=>`<button type="button" data-page="${i+1}" class="${i+1===page?'active':''}">${i+1}</button>`).join(''):'';}
pager?.addEventListener('click',e=>{const b=e.target.closest('button[data-page]');if(!b)return;page=Number(b.dataset.page)||1;render();scrollTo({top:Math.max(0,grid.offsetTop-110),behavior:'smooth'});});render();}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();