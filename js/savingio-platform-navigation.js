(function(){'use strict';const ITEMS=[{label:'홈',href:'/'},{label:'생활정보',href:'/articles/'},{label:'계산기',href:'/calculators/'},{label:'Savingio Lab',href:'/lab/',lab:true},{label:'사이트 탐색',href:'/categories/'},{label:'About',href:'/about.html'}];const path=location.pathname.replace(/index\.html$/,'');const esc=v=>String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));function ensureMaster(){if(document.querySelector('link[data-savingio-master]'))return;const link=document.createElement('link');link.rel='stylesheet';link.href='/css/savingio-master-template.css?v=20260723-master2';link.dataset.savingioMaster='true';document.head.appendChild(link);}function current(href){if(href==='/')return path==='/';return path.startsWith(href.replace(/index\.html$/,''));}function markup(cls){return `<nav class="${cls}" aria-label="Savingio 주요 메뉴">${ITEMS.map(i=>`<a href="${esc(i.href)}"${i.lab?' class="spn-lab"':''}${current(i.href)?' aria-current="page"':''}>${esc(i.label)}</a>`).join('')}</nav>`;}function install(){ensureMaster();const header=document.querySelector('.site-header .header-inner');if(!header)return false;const old=header.querySelector('.nav');if(old)old.remove();if(!header.querySelector('.savingio-platform-nav'))header.insertAdjacentHTML('beforeend',markup('savingio-platform-nav'));if(!header.querySelector('.savingio-mobile-nav-button')){const button=document.createElement('button');button.type='button';button.className='savingio-mobile-nav-button';button.textContent='전체 메뉴';button.setAttribute('aria-expanded','false');header.appendChild(button);document.body.insertAdjacentHTML('afterbegin',markup('savingio-mobile-nav-panel'));const panel=document.querySelector('.savingio-mobile-nav-panel');button.addEventListener('click',()=>{const open=panel.classList.toggle('is-open');button.setAttribute('aria-expanded',String(open));});panel.addEventListener('click',e=>{if(e.target.closest('a'))panel.classList.remove('is-open');});}return true;}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install,{once:true});else install();window.SavingioPlatformNavigation={install,items:ITEMS.slice()};})();