# MASTER LOG CURRENT

최종 갱신: 2026-07-17 20:49 KST

- 프로젝트: Savingio Factory
- 저장소: `yusun7749-art/savingio`
- 작업 브랜치: `main`
- 기준 코드 Git HEAD: `203c448 Production 검색·카테고리 검증 기록`
- 현재 상태: V3.020 CONTENT QUALITY AUDIT / NEXT BATCH BLOCKED
- 마지막 완료: 장기수선충당금 공식 근거 3건 기반 실제 E2E, Writer 반복 제거, 이미지→QA→CMS 자동 연결
- 마지막 완료: Savingio 인생 지도형 글 연결 리모델 5개 배치, 전체 220개 적용·검증 완료.
- 현재 상태: 220/220 life-map marker PASS, H1 PASS, 내부 깨진 링크 0.
- 다음 실행 위치: 누수 파일럿 3개를 약 5,000자 실제 정보 글과 실제 WebP/PNG 썸네일로 먼저 재작성·교체한다. 이 3개 Production 검증 전에는 다음 글 배치를 시작하지 않는다.
- 검색 통합 보완: 220개 전체의 제목·설명·Brain 계층·life-map 연결 제목·동의어를 하나의 인덱스로 재생성하도록 수정했다. 홈페이지/정보센터/Brain 검색이 같은 220개를 검색한다.
- 새 완료 기준: 본문 고정틀 + 상하위 연결 + 검색 동의어 + 홈페이지 검색 + 정보센터 검색 + Brain 검색 + CMS 자동 재색인 + QA.
- 검색 고정 기준: 메인·정보센터·Brain·글 내부는 `savingio-search-core.js`와 동일 인덱스를 사용한다. 명시적 별칭 > 제목 > 관련 키워드 > 제한적 오타 유사도 순으로 정렬한다.
- 누수 의도 분리: `누수/아랫집 누수/윗집 누수`는 긴급 대응 글, `일배책 누수`는 보험 글, `수도계량기 누수`는 자가진단 글을 우선한다.
- 레이아웃 계약: 전체 222개 글은 viewport와 승인된 스타일 소스를 가져야 한다. Factory `article-shell` 글은 `factory-article.css`를 사용하며 표·이미지·긴 URL은 화면 폭을 넘지 않게 처리한다.
- 신규 글 표준: Factory Writer는 장기수선충당금 글과 같은 `factory-article` 전체틀을 기본 출력하며, 이미지 엔진이 대표 썸네일과 인포그래픽을 연결한다.
- 공개 탐색 기준: `페이지 이동` 리디렉션과 `#U` 이스케이프 중복 경로는 URL 호환을 위해 파일은 유지하되 Brain·메인 검색·정보센터 색인에서는 제외한다. 실제 공개 검색 글 207개.
- 현재 작업: V3.020 누수 글 콘텐츠 완성도·실제 썸네일 정정 감사
- 다음 시작 위치: 누수 3개 격리 재작성 → 실제 이미지 생성·삽입 → 검색·카테고리·연결·계산기 검증 → Preview → Production 브라우저 검증
- Blocker: 누수 3개가 약 5,000자 기준을 충족하지 않고 실제 이미지 썸네일도 없음
- 마지막 검증: 검색·카테고리·레이아웃은 Production PASS. 그러나 누수 긴급 대응 1,486자, 일배책 누수 보험 1,242자, 누수 자가진단 1,340자(공백 포함 가시 본문)뿐이며 세 글 모두 `<img>` 0개다. 현재 물방울·방패·수도꼭지 영역은 `<figure>`와 CSS/이모지로 만든 제목 카드이지 실제 이미지 썸네일이 아니다. 따라서 V3.019의 '대표 썸네일 PASS'와 '파일럿 완성' 판정은 취소하며 UI 계약 PASS로만 제한한다.
- 공식 Publisher ID: `pub-7605193583747751` (LOCK)

## 다음 대화 확인 순서
1. 이 파일을 먼저 읽는다.
2. 오늘 요약은 `MASTER_LOG_PART1.md`에서 확인한다.
3. 시간대별 상세는 `MASTER_LOG_PART1-1.md`에서 확인한다.
4. 실제 수정 파일은 `MASTER_LOG_PART2.md`에서 확인한다.
5. 테스트 결과는 `MASTER_LOG_PART3.md`에서 확인한다.
## 다음 대화 첫 작업 — 반드시 이 순서로 진행
1. `MASTER_LOG_CURRENT.md`의 V3.020 정정 감사를 먼저 읽는다.
2. 다음 5개를 시작하지 말고 누수 3개만 고친다: `apartment-leak-emergency-response.html`, `daily-liability-leak-insurance.html`, `home-water-leak-self-check.html`.
3. 기존 글에 덧씌우지 않는다. 새 완성본을 격리 작성·검증한 뒤 기존 URL의 파일을 교체한다.
4. 각 글은 약 5,000자 실제 정보, 중복 없는 문단, 사례·체크리스트·FAQ·공식 근거·연관 흐름을 갖춰야 한다.
5. 실제 WebP/PNG 파일을 만들고 `<img>`, `alt`, `og:image`를 연결한다. CSS/이모지 카드는 썸네일 PASS로 인정하지 않는다.
6. 자가진단 글의 전기요금 계산기 오연결을 제거하고 누수·수도 흐름에 맞는 목적지만 연결한다.
7. 로컬 수치 검사와 Preview 후 Production에서 검색·왼쪽 카테고리·본문·실제 이미지·모바일/가로 넘침을 직접 확인한다.
8. 3개가 전부 PASS한 뒤 다음 5개로 진행하고, 안정성이 확인될 때만 20개→50개로 확대한다.
# V3.016 — 전체 글 리모델링 1차(20개) 적용

- 실제 공개 글 208개를 20개 단위로 검수하며 리모델링하기로 확정했다.
- 1차 20개는 관리비 → 장기수선충당금 → 누수 → 일배책 → 수도·공과금 → 전월세·주택세금 흐름으로 묶었다.
- 20개 전부에 공통 리모델링 CSS, 제목 기반 주제 썸네일, 사람이 다음에 찾는 연관 행동 경로를 적용했다.
- `장기충당금`, `아랫집에서 누수 연락`, `일배책으로 누수 보험 처리`처럼 실제 사용자 표현을 연결 제목에 포함했다.
- 리디렉션 호환 문서 14개는 공개 글/리모델링 대상에서 제외한다.
- 1차 전용 계약 검사와 기존 검색·레이아웃·Factory 검사를 통과한 뒤 배포한다.
